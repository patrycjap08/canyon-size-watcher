name: canyon-size-watcher

on:
  schedule:
    - cron: "*/10 * * * *"   # co 15 minut
  workflow_dispatch:         # pozwala uruchomić ręcznie z karty Actions

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Restore state cache
        uses: actions/cache@v4
        with:
          path: watch_state.json
          key: canyon-watcher-state-v1

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: pip install requests beautifulsoup4

      - name: Run watcher (FORCE_ALERT)
        env:
          FORCE_ALERT: "1"
        run: python watcher.py

      - name: Backup ping via curl (only when FORCE_ALERT)
        if: env.FORCE_ALERT == '1'
        run: |
          curl -H "Title: 🔔 Backup ping (curl)" -H "Priority: high" \
               -d "To jest zapasowy ping z workflow – jeśli to widzisz, kanał ntfy działa, a Python mógł się wywalić wcześniej." \
               https://ntfy.sh/gosia-canyon-alert

