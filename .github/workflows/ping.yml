name: ping-ntfy
on:
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send test push via ntfy
        run: |
          curl -H "Title: 🔔 Test z GitHub Actions" -H "Priority: high" \
               -d "Jeśli to widzisz, kanał ntfy działa." \
               https://ntfy.sh/gosia-canyon-alert
